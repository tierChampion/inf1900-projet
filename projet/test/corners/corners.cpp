#include "pins.h"

int main() {

    ReadPin a0 = ReadPin(Port::A, PA0);
    ReadPin a1 = ReadPin(Port::A, PA1);
    ReadPin a2 = ReadPin(Port::A, PA2);
    ReadPin a3 = ReadPin(Port::A, PA3);
    ReadPin a4 = ReadPin(Port::A, PA4);
    ReadPin a5 = ReadPin(Port::A, PA5);
    ReadPin a6 = ReadPin(Port::A, PA6);
    ReadPin a7 = ReadPin(Port::A, PA7);
}
